{
  "name": "automatic-verificator-of-html-and-md",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "check": "ls-lint && html-validate '*.html' && markdownlint '**/*.md' --ignore node_modules",
    "prepare": "husky install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^15.0.0",
    "@commitlint/config-conventional": "^15.0.0",
    "husky": "^7.0.4",
    "markdownlint-cli": "^0.30.0"
  },
  "dependencies": {
    "@ls-lint/ls-lint": "^1.10.0",
    "html-validate": "^6.1.3"
  }
}
